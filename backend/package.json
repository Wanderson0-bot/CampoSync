{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon --ignore data/ src/bootstrap.js",
    "start": "node src/bootstrap.js",
    "test": "cross-env NODE_ENV=test DB_DRIVER=json DATA_FILE=./data/camposync-test.json node --test tests/*.test.js",
    "test:watch": "cross-env NODE_ENV=test DB_DRIVER=json DATA_FILE=./data/camposync-test.json node --test --watch tests/*.test.js",
    "build": "node scripts/check.js",
    "preview": "node src/bootstrap.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mysql2": "^3.22.3",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
